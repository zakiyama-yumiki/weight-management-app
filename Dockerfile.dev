# Dockerfile.dev
FROM node:20-alpine

# 作業ディレクトリを設定
WORKDIR /app

# パッケージファイルをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm ci

# アプリケーションのソースコードをコピー
COPY . .

# ポートを公開
EXPOSE 5173

# 開発サーバーを起動（ホストを0.0.0.0に設定してDocker外からアクセス可能にする）
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]